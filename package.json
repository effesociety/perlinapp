{
	"name": "perlinapp",
	"version": "1.0.0",
	"description": "PerlinApp - The only app to play Perlina online",
	"scripts": {
		"start": "cd backend/src && npm start",
		"heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install --only=dev && npm install && npm run build"
	},
	"engines": {
		"node": "12.16.1"
	}
}